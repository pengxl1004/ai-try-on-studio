# Design Style: AI Virtual Try-on Studio

## 气质与意象

**意象锚点**：高端时装工作室的数字延伸 —— 想象一间极简白色工作室，落地窗洒入柔和自然光，裁缝台上整齐摆放着面料样本，墙上挂着完成的作品。一切井然有序，每个工具都在它该在的位置。

**关键词**：专业、精致、高效、克制优雅

这不是一个"炫酷科技风"的 AI 工具，而是一个为时尚从业者打造的**生产力工具**。它应该像一把好的裁缝剪 —— 握感舒适、使用精准、不喧宾夺主。

## 视觉策略

- **图像优先**：界面是画布，用户上传的服装和模特图才是主角。UI 元素退居幕后，用低饱和度中性色做底
- **卡片式布局**：每个功能区域用微妙的卡片分隔，卡片之间留足呼吸空间
- **状态可视化**：任务进度用渐进式色彩变化表达（灰→蓝→绿），不用突兀的弹窗打断工作流
- **微交互**：悬停时卡片微微上浮、按钮按下时轻微缩放、图片拖拽时半透明预览

## 配色方案

| 角色 | 色值 | 意象来源 |
|------|------|----------|
| 背景 | `#FAFAFA` | 工作室的白色墙面 |
| 卡片 | `#FFFFFF` | 裁缝台的白色台面 |
| 主色 | `#4F46E5` (Indigo-600) | 创意与专业的深靛蓝，像高级面料的色泽 |
| 主色浅 | `#EEF2FF` (Indigo-50) | 主色的极淡晕染，用于选中态背景 |
| 强调色 | `#F59E0B` (Amber-500) | 行动按钮的暖金色，像工作室的黄铜灯具 |
| 文字主 | `#1E293B` (Slate-800) | 深石板灰，比纯黑更柔和 |
| 文字次 | `#64748B` (Slate-500) | 辅助信息的低调灰 |
| 边框 | `#E2E8F0` (Slate-200) | 极淡的分隔线，若隐若现 |
| 成功 | `#10B981` (Emerald-500) | 完成状态的清新绿 |
| 错误 | `#EF4444` (Red-500) | 失败状态的警示红 |

## 字体排版

- **标题**：`Outfit` — 几何感强、现代但不冰冷，适合传达"精密工具"的气质
- **正文**：`Inter` — 屏幕阅读最优解，x-height 高，小字号也清晰
- **等宽**：`JetBrains Mono` — 用于任务编号、API 配置等技术信息

**排版节奏**：
- 页面标题：24px / 700 weight / tracking-tight
- 卡片标题：16px / 600 weight
- 正文：14px / 400 weight / leading-relaxed
- 辅助文字：12px / 500 weight / uppercase tracking-wider

## 动效与交互

- **缓动曲线**：`cubic-bezier(0.16, 1, 0.3, 1)` — 快入慢出，像物体自然减速
- **悬停上浮**：`translateY(-2px)` + 阴影加深，200ms
- **按钮点击**：`scale(0.97)`，100ms，给人"按下"的触感
- **图片拖拽**：拖拽中的图片 `opacity: 0.7` + `scale(1.05)`，落位时弹性回弹
- **进度条**：宽度过渡 500ms ease-out，完成时轻微脉冲

## 设计禁忌

- ❌ 不用渐变背景或彩虹色 —— 这是工具，不是玩具
- ❌ 不用超过 2px 的圆角（除了图片预览可以用 8px）—— 保持精密感
- ❌ 不用大面积纯黑或纯白对比 —— 用灰度层次营造深度
- ❌ 不用弹窗打断用户工作流 —— 用内联提示和侧边抽屉
- ❌ 不用过多图标堆砌 —— 文字能说明白的就不用图标
- ❌ 不用动画超过 500ms —— 工具的效率感不能被动画拖慢
